# Gemini仕様書の精査レビューと修正内容

実装前にGemini Deep Research版の仕様書を精査し、**実際にこの環境で動かして検証**した結果です。
「忖度なし」の指摘として、致命的なバグ2件・設計上の問題4件・注意点5件を確認しました。
数値基準そのものは概ね妥当でしたが、**データ取得まわりの仕様は そのまま実装すると壊れる箇所**がありました。

---

## 1. 致命的な問題（そのまま実装すると誤動作・動作不能）

### 1-1. 配当利回りの単位が間違っている【実測で確認済み】

仕様書: `dividendYield >= 0.02 (2.0%)`

**yfinance の現行版（この環境の 1.4.1 を含む 0.2.5x 以降）は `dividendYield` を
パーセント表記で返します。** 実測値:

| 銘柄 | dividendYield の生値 | 実際の利回り |
|---|---|---|
| AAPL | 0.37 | 0.37% |
| KO | 2.54 | 2.54% |
| 7203.T (トヨタ) | 3.55 | 3.55% |

つまり仕様書の `>= 0.02` 判定では**配当0.37%のAAPLですら「高配当」として通過**し、
配当フィルターが事実上無効になります。Geminiの仕様は古いyfinance（小数表記時代）の
知識で書かれています。

**修正**: バージョン依存を避けるため「年間配当額(trailingAnnualDividendRate) ÷ 株価」で
再計算する方式を第一とし、閾値は「2.0（%）」のようにパーセント単位で管理
（`fundamentals.py` の `_normalized_div_yield_pct`）。

### 1-2. pandas_ta はこの環境で動かない

仕様書: 「テクニカル指標計算は pandas_ta を使用」

pandas_ta はメンテナンスが停止しており、**numpy 2.x で `numpy.NaN` の import エラーが
発生**します。この環境は pandas 3.0 / numpy 2.x のため、仕様通りに実装すると
インストール段階または実行段階で確実に失敗します。

**修正**: SMA / RSI(Wilder方式) / MACD / CCI / スローストキャスティクスを純pandasで
自前実装（`indicators.py`）。定義は TradingView・証券会社ツールと同じ標準仕様で、
pandas_ta の計算結果と一致します。依存が減るため将来のライブラリ更新でも壊れにくい
構成です。

---

## 2. 設計上の問題（動くが品質・性能・実用性に難）

### 2-1. S&P 500 のティッカー表記変換が抜けている

S&P 500 には BRK.B、BF.B のような「.」入りシンボルがあり、yfinance では
**「-」に変換（BRK-B）しないと取得に失敗**します。仕様書はこれに言及していません。
→ `universe.py` で変換処理を実装。

### 2-2. 全銘柄に .info を取りに行く設計は遅く、429エラーの主因

仕様書は「.info をループ取得する際に sleep を挟む」とだけ書いていますが、
S&P500 + 日経225 = 約730銘柄 × 0.4秒 ＝ **約5分の待ち時間が毎回発生**し、
それでもブロックのリスクが残ります。

**修正**: **2段階スクリーニング**に変更。先に一括ダウンロードした価格データで
テクニカル条件を判定し、**通過した銘柄（実測で730→約190銘柄）だけ** .info を取得。
実行時間はフル実行で約3分、429リスクも大幅に低減しました。

### 2-3. 流動性を「出来高株数」で判定するのは不適切

仕様書: 「US 50万株以上 / JP 10万株以上」

出来高株数は株価水準で意味が変わります（株価100円の10万株=1,000万円、
株価3万円の10万株=30億円）。特に日本株は値がさ株と低位株の差が極端です。

**修正**: **売買代金（終値×出来高の20日平均）**基準に変更。
日本株 3億円/日以上、米国株 1,000万ドル/日以上。

### 2-4. バリュー条件に「バリュートラップ」対策がない

低PER・低PBR・高配当だけで絞ると、**万年割安株（収益性が低いまま放置されている
銘柄）が大量に混入**します。東証のPBR改革の文脈でも、PBRはROEとセットで見るのが
現在の標準です。

**修正**: `ROE >= 8%` を追加（`config.py` で変更・無効化可能）。
「割安だが稼ぐ力はある」銘柄に限定する効果があります。

---

## 3. 数値基準の妥当性検証（妥当だった点も含む）

| 項目 | 仕様値 | 判定 | 根拠 |
|---|---|---|---|
| US PER ≤ 18 | 妥当 | ○ | S&P500平均PER（約20台半ば）より十分下。割安圏として機能 |
| JP PER ≤ 14 | 妥当 | ○ | 東証プライム平均（16前後）より下 |
| US PBR ≤ 2.5 | 妥当 | ○ | S&P500平均（4超）より大幅に下 |
| JP PBR ≤ 1.2 | 妥当 | ○ | 解散価値近辺。東証平均（1.4前後）より下 |
| US 配当 ≥ 2% | 妥当 | ○ | S&P500平均（1.3%前後）の約1.5倍 |
| JP 配当 ≥ 3% | 妥当 | ○ | 東証平均（2%前後）の1.5倍。高配当株の一般的な目安 |
| バリューRSI 40–65 | 妥当 | ○ | 「下落中でも過熱でもない」帯として標準的 |
| グロース売上成長 ≥ 15% / EPS ≥ 20% | 妥当 | ○ | CAN-SLIM等の標準的な高成長基準と整合 |
| グロースRSI 65–85 + CCI ≥ 100 + ストキャ80×80 | **厳しすぎ** | △ | 後述 3-1 |
| 25日/50日SMA | 許容 | △ | 日本の慣行は25/75だが論理は成立。config化済み |

### 3-1. グロース条件の同時成立は極めて厳しい【フル実行で確認】

RSI 65–85、CCI ≥ 100、ストキャス %K・%D 両方80以上、終値>25SMA、乖離20%以内を
**同時に**満たす銘柄は、本日のフル実行で **米国株1銘柄・日本株0銘柄** でした。
「強いシグナルが揃った銘柄だけを厳選する」設計としては成立していますが、
該当ゼロの日が多くなることは理解して使う必要があります。

→ 対策として `--loose` プリセット（RSI 60–90、CCI ≥ 80、ストキャ70以上など）を実装。
「惜しい銘柄」をウォッチリストとして観察する用途に使えます。

### 3-2. revenueGrowth / earningsGrowth の意味に注意

yfinance のこの2項目は**「直近四半期の前年同期比」**であり、通期成長率ではありません。

- 前年同期が低水準だと異常な高成長率が出る（低ベース効果）
- 赤字→黒字転換は値が取得できない（None）→ 本ツールでは判定不能としてスキップ
- 金融株は revenueGrowth 自体が欠損することが多い（実測: 8306.T は None）
  → グロースタブから自然に除外されるが、これは許容範囲

### 3-3. 日本株の PER は「実績ベース」

yfinance の trailingPE は実績PERです。日本の証券アプリで一般的な
**会社予想ベースのPER とは異なる**ため、四季報・証券アプリの数値と差が出ることが
あります（出力には forwardPE も内部保持。正確な予想PERが必要なら後述の J-Quants 推奨）。

---

## 4. データソースの評価と代替案

### yfinance（現行採用）の評価

| 観点 | 評価 |
|---|---|
| コスト | ◎ 無料・APIキー不要 |
| 日米両対応 | ◎ ティッカー末尾 .T で日本株も取得可 |
| 株価データ品質 | ○ 日次OHLCVは実用十分 |
| ファンダ品質 | △ 日本株の決算反映が遅い・欠損あり。実績PERのみ |
| 安定性 | △ **非公式API**。仕様変更が頻繁（dividendYield単位変更が実例）。レート制限あり |
| 利用規約 | △ 個人利用は事実上黙認されているが、商用利用・再配布は不可 |

**結論: 個人の日次スクリーニング用途なら yfinance で十分実用**。ただし上記の弱点を
コード側で吸収する設計（単位正規化・2段階取得・リトライ・キャッシュ）が必須で、
本実装はすべて対応済み。

### 代替・補完API の比較

#### 日本株

| サービス | 費用 | 特徴 | 推奨度 |
|---|---|---|---|
| **J-Quants API**（JPX公式） | 無料(12週遅延) / ライト月1,650円 | 取引所公式。財務諸表・予想含む。データ品質最高 | ★★★ 本格運用なら最有力 |
| EDINET API（金融庁） | 無料 | 有報XBRL生データ。加工コストが非常に高い | ★ 上級者向け |
| kabuステーションAPI等 | 口座保有者無料 | リアルタイム。デイトレ向き | ★ 用途が違う |

#### 米国株

| サービス | 費用 | 特徴 | 推奨度 |
|---|---|---|---|
| **Financial Modeling Prep** | 無料250req/日〜 | 財務・スクリーナーAPI充実 | ★★★ 補完先の第一候補 |
| Finnhub | 無料60req/分 | ファンダ+ニュース | ★★ |
| Tiingo | 無料枠あり | 調整済み株価の品質良 | ★★ |
| Alpha Vantage | 無料25req/日 | **少なすぎてスクリーニング不可** | ★ 非推奨 |
| Polygon.io / EODHD | 有料 | 品質最高クラス | 予算次第 |

### 推奨ロードマップ

1. **現在**: yfinance のみ（本実装）— 無料・即日運用可能
2. **次の一手**: J-Quants ライトプラン導入 → 日本株の PER/PBR/ROE を会社予想・
   公式財務ベースに置換（`fundamentals.py` を差し替えるだけで対応できる構造済み）
3. **米国株強化**: FMP無料枠で通期成長率・アナリスト予想を補完

---

## 5. その他の追加実装（仕様書になかった改善）

- **スコアリング（0–100点）**: 条件通過銘柄を「どれだけ余裕を持って通過したか」で
  採点し降順表示。初心者がどれから見ればよいか分かる。計算式は透明（screener.py）
- **CSV出力**: results/ に utf-8-sig で保存（Excelで文字化けしない）
- **当日キャッシュ**: 同日の再実行は価格データを再ダウンロードしない
- **鮮度チェック**: 最終取引日が7日超過去の銘柄（上場廃止・取引停止）を自動除外
- **新形式銘柄コード対応**: 285A のような英字入りコード（2024年〜）に対応
- **`--universe-file`**: 自分のウォッチリストでスクリーニング可能
- **免責表示**: 投資助言ではない旨を出力

---

## 6. 検証結果サマリー（2026-06-12 実行）

- ユニバース: S&P500 503銘柄（Wikipedia取得成功）+ 日経225 225銘柄（同）
- フル実行時間: 約3分
- バリュー: 米14銘柄 / 日2銘柄 — 金融・公益・不動産中心（低PBR×高配当の条件から
  妥当な顔ぶれ）
- グロース: 米1銘柄 / 日0銘柄 — 仕様通りの厳しさ（3-1参照）
- 全条件・単位の整合性を実データで確認済み

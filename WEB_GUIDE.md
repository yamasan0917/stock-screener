# 「本日のおすすめ銘柄」ウェブ公開ガイド

CLI版スクリーナーをウェブサイト化したものです。
**事前計算方式**（バッチが結果JSONを生成 → 静的サイトが即表示）なので、
閲覧者は待ち時間ゼロでアクセスできます。サーバー費用も不要です。

```
generate_data.py（1日2回自動実行）
   ↓ docs/data/latest.json を生成
docs/ フォルダ = ウェブサイト本体（GitHub Pagesでそのまま公開）
```

---

## なぜ「リアルタイム更新」にしないのか（更新頻度の設計）

このツールの判定はすべて**日足（その日の終値）ベース**です。つまり:

- 日本株のデータが変わるのは **1日1回、大引け（15:30）のあと**
- 米国株のデータが変わるのは **1日1回、NY引け（日本時間の朝5〜6時）のあと**

ザラ場中に何度更新しても結果は変わらないため、**1日2回の自動更新が最適解**です。

| タイミング | 内容 |
|---|---|
| 平日 16:30 JST | 東京市場の大引け後 → 日本株の結果を更新 |
| 平日 朝 7:30 JST | NY市場の引け後 → 米国株の結果を更新 |

閲覧者は事前計算済みのJSONを読むだけなので、表示は一瞬です。

---

## 公開手順（GitHub Pages / 無料・自動更新つき）

### 1. GitHubリポジトリを作る

1. https://github.com/new で新しいリポジトリを作成（例: `stock-screener`、Publicを選択）
2. この `stock_screener` フォルダをリポジトリのルートとしてプッシュ:

```powershell
cd "C:\Users\Owner\Desktop\claude\ツール開発\01_dejiina_agent\stock_screener"
git init
git add .
git commit -m "initial commit"
git branch -M main
git remote add origin https://github.com/<あなたのユーザー名>/stock-screener.git
git push -u origin main
```

### 2. GitHub Pages を有効化

1. リポジトリの **Settings → Pages** を開く
2. Source: **Deploy from a branch**、Branch: **main**、フォルダ: **/docs** を選択して Save
3. 数分後に `https://<ユーザー名>.github.io/stock-screener/` で公開されます

### 3. 自動更新を確認

- `.github/workflows/update-data.yml` が平日2回、自動でスクリーニングを実行し
  `docs/data/latest.json` を更新します（設定不要・無料枠内）
- 手動で今すぐ更新したい場合: リポジトリの **Actions → update-data → Run workflow**

### 補足

- **無料・APIキー不要**で完結します（GitHub Actionsの無料枠は月2,000分、
  1回約15分 × 月44回 ≒ 660分で余裕）
- Yahoo Financeの非公式取得を使うため、**個人利用・小規模公開の範囲**で
  運用してください（広告収益化などの商用利用は規約リスクがあります）

---

## ローカルで確認する方法（公開前のプレビュー）

```powershell
cd "C:\Users\Owner\Desktop\claude\ツール開発\01_dejiina_agent\stock_screener"
python generate_data.py          # データ生成（約10〜15分）
cd docs
python -m http.server 8000       # → ブラウザで http://localhost:8000 を開く
```

※ `index.html` をダブルクリックで直接開くと、ブラウザのセキュリティ制限で
JSONが読めないことがあります。必ず上記のローカルサーバー経由で見てください。

ローカル運用だけで使う場合は、Windowsタスクスケジューラに
`python generate_data.py` を平日16:30と7:30に登録すればOKです。

---

## 対象銘柄（ユニバース）

| 市場 | 構成 | 銘柄数 |
|---|---|---|
| 米国株 | S&P500（大型）+ S&P400（中型）+ NASDAQ-100 + ウォッチリスト | 約1,040 |
| 日本株 | TOPIX500（大型・中型）+ 東証グロース市場全銘柄 + 日経225 | 約1,100 |

- **Oklo（OKLO）のような指数未採用の注目グロース株**は `watchlist_us.txt` でカバー
  しています（メモ帳で自由に追加・削除可能）。SMR・宇宙・量子・AIなど約35銘柄を
  プリセット済み
- 日本株は東証グロース市場を丸ごと含むため、新興グロース株も自動で対象になります。
  小型で売買が薄い銘柄は流動性フィルター（売買代金3億円/日）で自動除外されます
- 銘柄リストは Wikipedia / JPX公式から週1回自動更新されるため、指数の入れ替えにも
  自動で追随します

## 表示名について

- 日本株: JPX公式の上場銘柄一覧から**日本語社名**を自動取得（全銘柄対応）
- 米国株: 主要約260社は**カタカナ表記**（`names_ja.py` の辞書、自由に追記可能）。
  辞書にない銘柄は英語名のまま表示されます

## ウェブ版の機能一覧

- 市場（日/米）× 戦略（バリュー/グロース/ディフェンシブ）のタブ切替
- シナリオ別おすすめ（中東情勢・米中間選挙・FRB・ドル円の予想を選ぶと対応銘柄を表示。
  銘柄リストは `scenarios.py` で編集）
- 「標準 / ゆるめ」基準の切替（該当ゼロの日も惜しい銘柄をウォッチできる）
- スコア順表示・全列ソート・銘柄名検索
- 3ヶ月ミニチャート（スパークライン）
- ★お気に入り（ブラウザに保存）と★のみ表示
- Yahoo!ファイナンス / かぶたん / TradingView への外部リンク
- 初心者向け「表の見方」ガイド（PER・RSIなどの用語解説つき）
- スマホ対応・ダークモード自動対応
- 免責事項の表示
